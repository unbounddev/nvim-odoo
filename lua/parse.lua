local function parse_manifest(file, pos)
  local row, col = unpack(pos)
  -- local r = 1
  -- local ast = {}
  -- while (r <= row)
    -- for i = 1, #file[r] do 
    -- end
  -- end
end

return {
  parse_manifest = parse_manifest
}
